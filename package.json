{
  "name": "create-with",
  "version": "0.4.0",
  "description": "`npm init pkg <pkg-name>` core module",
  "repository": "allenhwkim/create-with",
  "scripts": {
    "build": "rimraf dist && webpack"
  },
  "engines": {
    "node": ">=7"
  },
  "bin": {
    "create-witn": "index.js"
  },
  "files": [
    "index.js"
  ],
  "dependencies": {
    "fs-extra": "^6.0.1",
    "inquirer": "^6.0.0",
    "mustache": "^2.3.0"
  },
  "devDependencies": {
    "node-fetch": "^2.1.2"
  }
}
